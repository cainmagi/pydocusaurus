---
id: RendererPage
title: RendererPage
sidebar_label: RendererPage
slug: /apis/renderer/page/RendererPage
description: 用来渲染 API 页面的渲染器。
---

import APITopBar, {IconObjType} from "@site/src/components/APITopBar";
import InlineIcon from "@site/src/components/InlineIcon";
import vsiCheckIcon from "@iconify-icons/codicon/check";

<APITopBar type="class" isDataClass={true} source="renderer.page.RendererPage" />

```python
RendererPage(
    data: DocModule | DocFunction | DocClass | DocDataClass | DocEnum | DocTypedDict | DocProtocol | DocTypeAlias
)
```

用来渲染 API 页面的渲染器。

## 字段 {/* #fields */}

<div className="long-type-table">

| 字段 | 类型 | 必选 | <center>说明</center> |
| :-----: | :-----: | :-----: | :------ |
| `data` | `DocModule \| DocFunction \| DocClass \| DocDataClass \| DocEnum \| DocTypedDict \| DocProtocol \| DocTypeAlias` | <InlineIcon icon={vsiCheckIcon}/> | 用来渲染页面的数据。 |

</div>

## 方法 {/* #methods */}

### <IconObjType type="method"/> `render` {/* #-render */}

```python
v_1: str = self.render()
```

将页面渲染成 MDX 文件。

#### 输出 {/* #returns */}

| 参数 | 类型 | <center>说明</center> |
| :-----: | :-----: | :------ |
| `v_1` | `str` | 按 MDX 格式文字所渲染的页面。 |

## 属性 {/* #properties */}

### <IconObjType type="param"/> `file_path` {/* #-file_path */}

```python
file_path: str = self.file_path
```

获取用来保存页面内容的输出文件路径。

______________________________________________________________________

### <IconObjType type="param"/> `id` {/* #-id */}

```python
id: str = self.id
```

获取该页面的 ID。

______________________________________________________________________

### <IconObjType type="param"/> `metadata` {/* #-metadata */}

```python
metadata: dict[str, str] = self.metadata
```

包含了 Docusaurus 元数据的 YAML 文件头。

______________________________________________________________________

### <IconObjType type="param"/> `short_descr` {/* #-short_descr */}

```python
short_descr: str = self.short_descr
```

页面的短描述文字。该内容提取自完整信息的第一句话。

______________________________________________________________________

### <IconObjType type="param"/> `slug` {/* #-slug */}

```python
slug: str = self.slug
```

用作文档地址的短链接。
